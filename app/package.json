{
  "name": "zbar-wasm-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:wasm": "mkdir -p public/wasm && cp ../dist/zbar.wasm public/wasm/zbar.wasm",
    "dev": "npm run prepare:wasm && vite",
    "build": "npm run prepare:wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.0",
    "vite": "^5.3.0"
  }
}
